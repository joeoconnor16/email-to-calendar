# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'email-to-calendar' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for email-to-calendar
	pod ‘p2.OAuth2’
	pod ’SwiftyJSON’

  target 'email-to-calendarTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'email-to-calendarUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
